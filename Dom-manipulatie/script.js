const btnToggleName = document.getElementById("btnToggleName");
const nameContainer = document.getElementById("nameContainer");
const myName = "Hendrik";
btnToggleName.addEventListener("click", toggleName);

function toggleName() {
    nameContainer.innerHTML = "Hi Hendrik";
}

const colors = ['red', 'red', 'green', 'yellow', 'orange'];
colors.forEach(function(number);
document.getElementById("nameContainer").innerHTML = Math.random();



